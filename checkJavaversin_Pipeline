pipeline {
    agent any

    stages {
        stage('check jova version') {
            steps {
                echo 'try to check java version'
                sh "java --version"
            }
        }
    }
}

